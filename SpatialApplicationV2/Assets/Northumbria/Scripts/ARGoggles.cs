using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class ARGoggles : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //File.WriteAllText(Application.persistentDataPath + "/full", "8128471");
        //File.WriteAllText(Application.persistentDataPath + "/backup", "5467575");
    }

    // Update is called once per frame
    void Update()
    {

    }

    //private void OnCollisionEnter(Collision collision)
    //{
    //    Debug.Log(collision.gameObject);
    //}
}
